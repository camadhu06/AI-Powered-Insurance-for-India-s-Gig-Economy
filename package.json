{
  "name": "gigware-ecosystem",
  "version": "1.0.0",
  "description": "GigWare Full Architecture Runtime",
  "scripts": {
    "dev": "concurrently -n \"ML,API,ADMIN,APP\" -c \"bgBlue.bold,bgGreen.bold,bgMagenta.bold,bgYellow.bold\" \"npm run ml\" \"npm run backend\" \"npm run admin\" \"npm run app\"",
    "ml": "cd ml && uvicorn main:app --reload --port 8000",
    "backend": "cd backend && npm start",
    "admin": "cd admin-web && npm start",
    "app": "cd user-app && npx expo start"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}