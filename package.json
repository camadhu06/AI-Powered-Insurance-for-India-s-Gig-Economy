{
  "name": "ai-powered-insurance-for-indias-gig-economy",
  "version": "1.0.0",
  "description": "Root package to run all services concurrently",
  "scripts": {
    "install-all": "npm install --prefix backend && npm install --prefix admin-web && npm install --prefix user-app",
    "start": "npx concurrently \"npm start --prefix backend\" \"npm start --prefix admin-web\" \"npm start --prefix user-app\""
  }
}
